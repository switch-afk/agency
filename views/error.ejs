<%- include('partials/header') %>
<div class="layer min-h-screen flex items-center justify-center px-4">
  <div class="text-center max-w-lg">
    <div class="font-display font-extrabold text-glow mb-4 text-brand-core" style="font-size:8rem;line-height:1;opacity:0.3"><%= code %></div>
    <h1 class="font-display font-extrabold text-2xl sm:text-3xl text-white mb-3"><%= message %></h1>
    <p class="text-slate-400 mb-8 text-sm sm:text-base"><%= sub %></p>
    <a href="<%= back %>" class="inline-block font-mono text-xs px-8 py-3 rounded-sm text-white" style="background:#1a6fff">← GO BACK</a>
  </div>
</div>
<%- include('partials/footer') %>
